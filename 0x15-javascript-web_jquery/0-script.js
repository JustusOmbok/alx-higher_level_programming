document.addEventListener("DOMContentLoaded", function() {
 // Waits for dom to load before accessing
 const header = document.querySelector("header");
 if (header) {
    header.style.color = "#FF0000";
 }

});
